# Formulario

#### Esto es lo que hice dentro del **html** y **css**, gracias a los ejercicios dados por el maestro. 
---

*PRIMER RETO*
---
En este lo primero que hice fue conectar el **html** junto al **css**: `styles.css`. Después continúe con un **pseudoclase**: root al cual le cambie su color y realicé lo mismo con los demás. En el **--radio** cambié los **px** para lograr un estilo redondo en los bordes.

*SEGUNDO RETO*
---
 En `index.html` busque la mitad entre asunto y mensaje, luego utilicé un **div**  con la clase **.campo** para realizar un `<label>` junto un  `<input>` para conocer la edad de las personas que ejecuten este formulario, ya que no era **texto** utilicé: **type="number"**, y hay una regla: **min** hasta el **15** y un **max** hasta **99**.

 *TERCER RETO*
 ---
` box-shadow` en este lo que hice fue jugar para alcanzar una sombra que se refleje en el boton **Enviar mensaje**.